# Copy only after verifying the installed Codex configuration schema.
[agents]
max_concurrent_threads_per_session = 6
# A4 routes enforce one writer, two-to-four read-only workers, no recursion,
# and at most six total active workers through the current routing profile.

[features]
hooks = true

