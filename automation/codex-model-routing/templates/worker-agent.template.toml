name = "project_worker"
description = "Use only through the TOKEN-OPT current routing profile and matching contract."
model = "REPLACE_WITH_PROFILE_MODEL"
model_reasoning_effort = "REPLACE_WITH_SUPPORTED_PROFILE_EFFORT"
sandbox_mode = "read-only"
developer_instructions = """
Read-only workers use the shared Luna/Ox contract: do not write, delegate, spawn
workers, duplicate ordinary work, expose secrets, or start the next slice. Return
bounded facts, inferences, unverified items, material findings, and a no-write
attestation. Use the separate Terra writer contract only for an explicitly routed
single writer.
"""
