# MAEOS worktree lifecycle

Worktree budget begins at zero. Only the root may create, adopt, move, integrate, or remove a task worktree, and only after a verified isolation need plus a finite permit. Do not create worktrees per child. Preserve unknown worktrees and report an exact blocker instead of cleanup.
