# MAEOS task graph orchestration

Use a finite root-authored graph for non-trivial fan-out and every burst beyond four readers. Each node declares objective, dependencies, semantic role, read/write mode, owned paths/read scope, exclusions, inputs, deliverable, verification, and stop condition. Graphs do not authorize workers, writes, worktrees, or recursion.
